import AppShell from "../../components/AppShell";

export default function ProfilePage() {
  return (
    <AppShell>
      <h2 className="text-4xl font-bold">My Parking Info</h2>
      <p className="mt-3 text-base text-muted">
        Profile Settings page stub. Account and preference controls will be added in
        a future update.
      </p>
    </AppShell>
  );
}
