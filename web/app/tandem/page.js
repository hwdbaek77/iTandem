import AppShell from "../../components/AppShell";

export default function TandemPage() {
  return (
    <AppShell>
      <h2 className="text-4xl font-bold">Find Tandem Match</h2>
      <p className="mt-3 text-base text-muted">
        Find Tandem page stub. Tandem matching and scheduling will be added in a
        later iteration.
      </p>
    </AppShell>
  );
}
