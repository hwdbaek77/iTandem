import AppShell from "../../components/AppShell";

export default function CarpoolPage() {
  return (
    <AppShell>
      <h2 className="text-4xl font-bold">Carpool Matching</h2>
      <p className="mt-3 text-base text-muted">
        Find Carpool page stub. Matching cards and filters will be added in a later
        iteration.
      </p>
    </AppShell>
  );
}
